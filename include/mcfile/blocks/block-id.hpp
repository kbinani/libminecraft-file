#pragma once

namespace mcfile {
namespace blocks {

/// @typedef BlockId
/// @brief BlockId
using BlockId = uint32_t;

enum : BlockId {
    unknown = 0,
};

} // namespace blocks
} // namespace mcfile
